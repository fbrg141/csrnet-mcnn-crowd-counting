"""Visualization helpers placeholder."""


def describe_visualization_plan() -> str:
    return (
        "Visualize sample images, annotations, density maps, and predicted counts "
        "after dataset inspection and baseline training."
    )
